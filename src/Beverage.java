/**This interface has two methods, order and get calories.
 * order returns a print statement for what the user ordered,
 * and calories returns a static int of how many calories
 * each beverage choice has.
 * */
public interface Beverage {
    void order();
    int getCalories();
}
